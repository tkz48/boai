//! Generates the reward for the function which we required
//! This is a very thin client and only takes care of the formatting and nothing else
//!
//! We do the data preparation outside of the tool scope

pub(crate) mod client;
